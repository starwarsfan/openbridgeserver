# The contents of this file is based on https://github.com/home-assistant/core/blob/dev/pyproject.toml

target-version = "py313"

line-length = 150

#[lint]
#select = [
#    "ALL",
#]

#ignore = [
#    "D203", # no-blank-line-before-class (incompatible with formatter)
#]

# Relaxed rules for tests
[lint.per-file-ignores]
"tests/**" = [
    "S101",    # Use of assert detected
    "ANN",     # Type annotations
    "D",       # Docstrings
    "SLF001",  # Private member access
    "PLR2004", # Magic values
    "E402",    # Module level import not at top of file (common in test helpers)
]
# Contract tests use pytest.importorskip() before importing the library under test,
# which places non-import statements between imports — a standard pytest pattern.
"tests/contracts/**" = [
    "E402",    # Module level import not at top of file
]

[lint.flake8-bugbear]
# FastAPI's dependency-injection style requires calling Depends()/Query()/etc. as
# argument defaults; these are framework singletons, not mutable-default bugs (B008).
extend-immutable-calls = [
    "fastapi.Depends",
    "fastapi.Query",
    "fastapi.Path",
    "fastapi.Body",
    "fastapi.Cookie",
    "fastapi.Header",
    "fastapi.Form",
    "fastapi.File",
    "fastapi.Security",
]

[lint.flake8-pytest-style]
fixture-parentheses = false

[lint.pyupgrade]
keep-runtime-typing = true

[lint.mccabe]
max-complexity = 25
