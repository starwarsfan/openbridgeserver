{
  "name": "obs-help",
  "version": "0.0.0-dev",
  "description": "open bridge server integriertes Hilfesystem (VitePress)",
  "type": "module",
  "scripts": {
    "dev": "npm run help-index && vitepress dev",
    "build": "npm run help-index && vitepress build",
    "preview": "vitepress preview --port 4175",
    "help-index": "node scripts/generate-help-index.mjs",
    "test": "node --test scripts/*.test.mjs"
  },
  "devDependencies": {
    "vitepress": "^1.6.4"
  }
}
